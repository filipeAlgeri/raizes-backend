/**
 * globalTeardown — roda UMA vez após todos os testes.
 *
 * O banco raizes_db_test é mantido entre execuções e resetado no próximo
 * globalSetup. Não há nada a fazer aqui.
 */
module.exports = async function globalTeardown() {};
